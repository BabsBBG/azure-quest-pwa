# M5/M6 Defect Ledger

Date opened: 2026-07-26

Status: OPEN

No S1 or S2 defect may remain before M5 production hardening or Phase 6 completion sign-off.

| ID | Severity | Reporter | Route or subsystem | Expected behaviour | Actual behaviour | Root cause | Fix | Retest result | Final status |
|---|---|---|---|---|---|---|---|---|---|
| M5M6-DEF-001 | S1 | Principal Engineer, Product QA | `/admin` | Admin routes require authenticated role checks before protected data/actions render. | `/admin` was publicly routable and presented protected admin language/actions without runtime auth/role gating. | Admin Review Studio was a static scaffold mounted directly in `App.tsx`. | Added `ProtectedAdminRoute`, denied anonymous/users before Admin renders, read server-backed `user_roles`, added static route validation, and added dev-only signed-in browser gates for regular-user denial and SUPPORT_ADMIN support-only access. | Live browser verification on 2026-08-03 passed: USER is denied `/admin`, while MAIN_ADMIN, CONTENT_REVIEWER, and SUPPORT_ADMIN can enter the protected Admin route. | Fixed and live-verified for route access |
| M5M6-DEF-002 | S1 | Principal Engineer | Source-grounded content | Trusted certification practice is served from live approved source-grounded records. | Approved records are local/static fixtures and the 600-question seed bank remains the practical learner bank. | M5 source pipeline is implemented as local scaffolding and migrations, not production ingestion/review/publish runtime; production quiz/run CTAs could still route toward demo-bank assessment paths when approved coverage was incomplete. | Added a production-content gate in repo: curated quizzes and finite runs remain blocked when approved source-grounded placements are incomplete, blocked production CTAs are disabled with explicit not-ready copy, Readiness no longer deep-links straight to seed-bank certification runs, and `validate:production-content` is wired into CI. Full live source ingestion/review/published content remains pending. | `npm run validate:production-content` and `npm run typecheck` pending. | Partially fixed in repo; live source content remains open |
| M5M6-DEF-003 | S1 | Security/Data/Governance QA | Supabase/RLS | Migrations are applied and RLS boundaries are live-tested with MAIN_ADMIN, CONTENT_REVIEWER, SUPPORT_ADMIN, USER_A, and USER_B. | Migrations are applied to `praxisgrid-production`; focused live RLS/role/two-user probes now pass for covered M5 tables and roles. | Live Supabase activation was previously blocked by missing owner/QA bootstrap; Google OAuth credentials remain absent. | Created `praxisgrid-production`, applied migrations `0001` through `0026`, configured auth redirects including `/auth?mode=update-password`, bootstrapped owner/study accounts, created temporary Reviewer/Support/User A/User B identities, added opt-in live production QA scripts, ran live role/RLS/two-user probes, then deleted temporary users and bounded QA rows. | Live verification passed on 2026-08-03: all returned public tables have RLS enabled; User A/B isolation passed 27 checks in each direction; USER cannot escalate or read audit; Reviewer/Support cannot publish; Support can read support reports; MAIN_ADMIN can read audit; `npm run validate:live-supabase` passed with exact project guards. | Fixed and live-verified for focused M5 role/RLS scope |
| M5M6-DEF-004 | S1 | Principal Engineer, Security QA | GitHub import | Repo import limits/cache are durable and scoped to authenticated users. | API uses in-memory maps and IP-based rate buckets that reset per server instance. | Serverless endpoint had no durable store/session-aware limiter. | Replaced process-memory cache/rate buckets with authenticated Supabase JWT verification, `github_import_events` per-user daily limit tracking, `github_import_cache` repo cache storage, server-side service-role-only writes, placeholder env names, and CI validation. | Focused GitHub import controls validator, authorization validator, client tests, typecheck, and lint passed locally. Live Supabase migration/env verification pending. | Fixed in repo; live verification pending |
| M5M6-DEF-005 | S1 | Product Designer, Product QA | Secondary practice surfaces | Every assessment-like public surface shows trust copy before start and hides answers/explanations until completion. | KQL Gym, Flashcards, and other secondary practice surfaces could show explanations immediately or lack the shared pre-start notice. | Secondary learning/practice routes were not brought under the same assessment trust contract. | Added `QuestionBankNotice` to KQL Gym, Case Files, Scenario Player, and Flashcards; changed KQL Gym to hide correctness/explanations until completion; removed celebratory confetti from KQL completion; added signed-in browser assertions for Flashcards, KQL, Cases, and Scenario Player trust copy. | Retested 2026-07-29. `npm run test:e2e:chromium`, `npm run test:e2e:webkit`, `npm run test:e2e:mobile`, and explicit public production smoke passed; production smoke skips the dev-only signed-in fixture by design. | Fixed in repo; live signed-in production auth verification pending |
| M5M6-DEF-006 | S2 | Product Designer | Mobile learner IA | Mobile learner navigation uses exactly Learn, Practise, Prove and never wraps at 320px. | Existing mobile nav remained crowded and still carried more destinations. | Old M1 navigation model remained active. | Replaced primary nav with Learn, Practise, Prove and moved Account to the header. | Focused route check, lint, build, and mobile Playwright gate passed locally. | Fixed in repo; live verification pending |
| M5M6-DEF-007 | S2 | Product Designer | Career Lab / Interview Studio | In-progress interview work is recoverable. | Active answers/timer/session state were component-local and persisted only on completion. | Career Lab lacked active session persistence and recovery flow. | Added `ActiveInterviewSession`, local/legacy storage keys, cloud sync helpers, owner-only `active_interview_sessions` RLS migration, Career Lab resume/discard UI, debounced answer/rubric/score draft persistence, timed checkpoints, completion cleanup, regression test, and CI validator. | `npm run validate:active-interview-recovery`, `npm test -- src/store/useAppStore.test.ts`, `npm run typecheck`, `npm run lint`, and `npm run build` passed locally. Browser refresh/resume and live Supabase verification pending. | Fixed in repo; browser/live verification pending |
| M5M6-DEF-008 | S2 | Product QA | Test gates | Browser, mobile, WebKit, axe, visual, and production smoke gates run in CI, with protected opt-in production auth smoke for live users. | Current QA was Vitest plus static validators; no Playwright/axe/visual/live production auth gate existed. | Runtime verification infrastructure had not been added. | Added Playwright configs, Chromium/WebKit/mobile/tablet/desktop projects, axe accessibility checks, visual-tagged auth layout checks, public-route routing checks, signed-in onboarding/Admin/assessment gates through a dev-only E2E auth harness, production-smoke config, CI workflow commands, and opt-in `test:browser-production-auth` for live production auth smoke with temporary users. | Retested 2026-08-03. CI suites passed through merged `main`; explicit public production smoke passed 20/20; `test:browser-production-auth` passed learner onboarding/admin denial/trust copy at 1280x900 and 390x844 plus SUPPORT_ADMIN report inspection without approve actions. Temporary users were deleted. | Fixed and focused live-verified; broader cross-device product sync still pending |
| M5M6-DEF-009 | S2 | Security/Data/Governance QA | Support role boundary | SUPPORT_ADMIN can access support queues without editorial publishing authority. | Content-quality report RLS is review/admin-oriented and did not cleanly expose a support queue boundary. | Role capabilities were not separated consistently across content-quality tables or Admin affordances. | Added `can_read_support_queue`, support-only read policies for report tables, guard triggers preventing SUPPORT_ADMIN report/content mutation, role-filtered Admin queues, support-only inspect affordance, Main Admin-only approve affordance, and `validate:support-boundary`. | `npm run validate:support-boundary`, `npm run typecheck`, and live `validate:live-supabase` role checks passed. SUPPORT_ADMIN can read support reports and cannot publish approved questions. | Fixed and live-verified for focused support boundary |
| M5M6-DEF-010 | S2 | Security/Data/Governance QA | Privacy workflows | Users can export/delete account-owned cloud data and repository analyses safely. | Local export/reset exists, but cloud export/delete workflows and verification are missing. | M3/M4 sync tables were added without full M6 privacy operations. | Added signed-in Settings actions for cloud export and cloud learning/repository-analysis deletion, added `exportCloudData` and `deleteCloudLearningData`, added owner delete RLS policies for learning/profile tables plus existing Project Intelligence/import delete policies, and added `validate:privacy-workflows`. | `npm run validate:privacy-workflows`, `npm run typecheck`, `npm run lint`, `npm test`, and `npm run build` passed locally. | Fixed in repo; live Supabase/RLS verification pending |
| M5M6-DEF-011 | S2 | Product/UX QA | Production auth | Public production has configured Supabase auth and no logged-out demo practice. | Production has Supabase env vars and focused live email auth now passes; Google SSO remains intentionally hidden until OAuth credentials exist. | Production env was previously missing and one deployed anon key value was malformed. | Added public `/auth` and protected learner shell, configured Vercel Production Supabase env, rotated malformed browser key material, configured Supabase Auth Site URL/redirects for the working fallback domain, and added exact recovery redirect URLs. | Live disposable auth verification passed: signup confirmation, sign-in, onboarding persistence, admin denial, logout, re-sign-in, recovery redirect, password update, and rotated-password sign-in. Owner reset email has been sent; owner completion remains pending. | Fixed and live-verified for email auth; Google SSO externally blocked |
| M5M6-DEF-012 | S3 | Principal Engineer | Bundle performance | Main production bundle is reasonably split. | Vite reports a main JS chunk above 500 kB. | Routes and admin surfaces are not lazy-loaded. | Pending. | Pending. | Open |
| M5M6-DEF-013 | S1 | Product Designer | Home IA | Home presents exactly three primary outcomes: Learn, Practise, Prove. | Home led with certification path cards and path-first decisioning. | Previous path-selection dashboard remained the root protected home surface. | Rebuilt `PathHome` so the first primary choices are exactly Learn, Practise, and Prove; moved certification progress to a secondary panel; added `validate:auth-first-ia`. | `npm run typecheck` passed locally. Full retest pending. | Fixed in repo; browser verification pending |
| M5M6-DEF-014 | S1 | Product Designer, Product QA | Assessment Shell and Results | Assessments use a separate focused shell and results use a compact analytical report. | `/arena` ran inside the learner shell and rendered results inside the same dashboard-style component; the submission review panel also stayed open after final submit, shadowing the results screen. | The original practice arena owned both runtime and result states inside normal learner chrome, and final submit set `finished` without closing `submissionReviewOpen`. | Added `ProtectedAssessmentRoute` so `/arena` is authenticated but outside learner layout; replaced the completed-state UI with an analytical report containing Back to Practise, JSON/CSV/print exports, retake, summary, score ring, threshold explanation, correct/total, time used, domain-performance table, recommendation, and expandable question review; added `validate:assessment-shell`; fixed final submit to close submission review before rendering results; added signed-in browser coverage for answer, review-before-submit, final submit, result exports, domain table, and question review. | Retested 2026-07-29. Focused signed-in assessment flow passed in Chromium and WebKit, then full Chromium 15/15, WebKit 15/15, mobile 30/30, accessibility 36/36, visual 6/6, build, and explicit public production smoke passed. | Fixed in repo; live signed-in production auth verification pending |
| M5M6-DEF-015 | S1 | Product Designer, Product QA | Account/Auth copy | Auth/account copy reflects mandatory account creation and does not promise logged-out demo practice. | Account copy said local demo practice was available without sign-in. | Earlier local-first auth copy was not updated after the mandatory-auth decision. | Replaced logged-out/local-demo language in Account with mandatory-account copy and added `validate:auth-first-ia` to prevent regression. | `npm run typecheck` passed locally. Full retest pending. | Fixed in repo; browser verification pending |
| M5M6-DEF-016 | S2 | Product Designer | Global typography | Body UI uses readable sans-serif typography with monospace reserved for code/technical metadata. | `src/styles.css` still makes the whole app monospace-first. | M1.6 visual direction used developer/security monospace globally; latest production UX brief supersedes it. | Switched root/body typography to a readable sans-serif stack, retained the existing monospace stack only through `code`, `kbd`, `samp`, `pre`, `.font-mono`, and `.aq-technical`, marked Career Lab content hashes as technical metadata, and added `validate:typography` to CI. | `npm run validate:typography`, `npm run typecheck`, `npm run lint`, `npm test`, and `npm run build` passed locally. | Fixed in repo; browser/design verification pending |
| M5M6-DEF-017 | S2 | Principal Engineer, Security QA | Project Intelligence | Imported repositories produce private durable analysis records with delete/regenerate/isolation flows. | Public repo import creates deterministic draft story data and cloud upserts, but no durable analysis job/workspace/deletion workflow exists. | M4 import scope stopped at README/language story drafting and did not implement Phase 6 Project Intelligence operations; analysis IDs were content-hash-based before cloud persistence and could collide globally if two users imported the same repo. | Added typed Project Intelligence analysis payloads with overview, evidence-backed architecture map, strengths, risks/improvements, and interview prompts; persisted analyses into owner-scoped `project_intelligence_analyses` with user-scoped analysis row IDs, RLS, and cascade delete; added owner delete policy for imported projects; added Career Lab review/regenerate/delete controls; removed named demo-project coaching references from the interview path; and added `validate:project-intelligence`. | `npm run validate:project-intelligence`, `npm run validate:project-fixtures`, `npm run typecheck`, `npm run lint`, `npm test`, and `npm run build` passed locally. Latest row-isolation hardening retest passed `npm run validate:project-intelligence` and `npm run typecheck`. | Fixed in repo; live Supabase/browser verification pending |
| M5M6-DEF-018 | S2 | Principal Engineer | CI production smoke clarity | CI clearly distinguishes skipped production smoke from passed production smoke. | Production smoke only runs on `main` with `PRODUCTION_BASE_URL`; branch CI can be green without live production smoke. | Workflow condition is correct for safety but the harness/reporting can be misread as a production-smoke pass. | Replaced the silent workflow-level condition with `scripts/run-production-smoke.mjs`: PRs without a production URL print `PRODUCTION_SMOKE_STATUS=SKIPPED_MISSING_BASE_URL`, `main` fails if the URL is absent, configured runs execute Playwright, and `playwright.production.config.ts` no longer falls back to the historical Azure Quest URL. Added `validate:production-smoke-gate` to CI. | `npm run validate:production-smoke-gate`, `npm run test:production-smoke` without a production URL, `npm run typecheck`, `npm run lint`, `npm test`, and `npm run build` passed locally. | Fixed in repo; production smoke still pending until canonical URL/env are configured |
| M5M6-DEF-019 | S2 | Security/Data/Governance QA | GitHub import quota | Public repo import daily limit is enforced atomically per authenticated user. | The endpoint counted existing import events and inserted a new event later, allowing concurrent requests to exceed the daily quota; live verification also exposed an ambiguous `import_day` reference in the quota RPC output. | Quota check and event write were split across two serverless/database operations; the first RPC version accepted `p_user_id` without execute-scope hardening, and the PL/pgSQL return column name conflicted with the event-table column during live execution. | Added `claim_github_import_quota` Supabase RPC with transaction-scoped advisory lock, moved the API to claim quota through that function for cached and fresh imports, added caller mismatch protection if execute is ever broadened, revoked execute from anon/authenticated, granted execute to service_role, updated the GitHub import validator, added migration `0027` to fix the output-column ambiguity, and added `test:github-import-production` to the live harness. | `npm run validate:github-import-controls`, `npm run validate:migrations`, production migration dry-run/apply for `0027`, remote migration history through `0027`, `npm run test:github-import-production`, and full opt-in `npm run validate:live-supabase` passed. | Fixed, applied live, and live-verified |
| M5M6-DEF-022 | S2 | Security/Data/Governance QA | Content-quality report audit events | Report audit events are append-only and cannot be updated or deleted by privileged users. | The original report-event policy granted `for all` to Main Admin, which made audit rows mutable. | Audit events were modelled as admin-managed data instead of append-only governance records. | Added migration `0025_rpc_and_audit_hardening.sql` to drop the `for all` policy and replace it with Main Admin insert/select-only event policies while preserving support/main read policies from the support boundary migration. Updated `validate:content-quality-reports`. | `npm run validate:content-quality-reports` and `npm run typecheck` passed locally. Live Supabase RLS verification pending. | Fixed in repo; live verification pending |
| M5M6-DEF-023 | S2 | Product/UX QA | Auth redirect continuity | Signing in from a protected deep link returns the user to the intended route after email sign-in or Google SSO. | Protected routes stored `state.from`, but AuthPage always navigated authenticated users to `/`, and Google OAuth redirected to `/account`. | Auth redirect handling did not preserve route intent through the public auth page or external OAuth redirect. | Added safe internal `from` query preservation from protected routes, AuthPage return-path sanitization, Google SSO redirect target support, `validate:auth-redirects`, and an OAuth redirect regression test. | `npm run validate:auth-redirects`, focused auth tests, and `npm run typecheck` passed locally. Full retest pending. | Fixed in repo; browser verification pending |
| M5M6-DEF-024 | S2 | Product/UX QA | Destructive data actions | Learners get a clear confirmation before deleting local progress or Project Intelligence analysis, and cloud delete failures are visible. | Local reset was a one-click destructive action, repository analysis delete had no explicit irreversible-action confirmation, and cloud analysis delete failures were swallowed after local removal. | Earlier local-first flows optimized for prototype speed rather than production privacy/operational safety. | Added explicit browser confirmations for local reset and repository analysis deletion, made repository analysis delete await the cloud delete result, surfaced delete failures in Career Lab, and added `validate:destructive-actions` to CI. | `npm run validate:destructive-actions` passed locally. Full suite and browser/live Supabase delete verification pending. | Fixed in repo; browser/live verification pending |
| M5M6-DEF-025 | S2 | Product/UX QA | Post-signup onboarding | New signed-in learners complete a brief setup before accessing Learn, Practise, Prove, or assessment routes. | Authenticated users could land directly in the workspace without choosing a starting path, goal, or experience level. | The mandatory-auth shell existed, but no onboarding gate or preference persistence had been added. | Added `/onboarding`, learner/assessment onboarding guards, safe deep-link return preservation, Supabase auth metadata persistence for starting cert/goal/experience, Account edit access, `validate:onboarding`, and a signed-in browser gate for protected-route onboarding redirect/completion. | `npm run validate:onboarding`, `npm run validate:routes`, `npm run typecheck`, and `npm run test:e2e:chromium` passed locally. Live Supabase provider verification pending. | Fixed in repo; live verification pending |
| M5M6-DEF-026 | S2 | Security/Data/Governance QA | Migration/RLS/isolation gates | CI includes umbrella checks for migration order, RLS boundary presence, repository-isolation contracts, and opt-in live production probes. | Individual validators existed, but the harness lacked named live production Supabase scripts. | Validation coverage grew organically per feature and did not include top-level live production gate names. | Added the three umbrella validators and wired them into CI after authorization checks. Added opt-in live scripts `validate:live-supabase`, `test:auth-production`, `test:live-rls`, `test:roles-production`, and `test:repository-isolation-production` with explicit production opt-in, exact project guards, temporary data cleanup, and JSON output. | `npm run validate:migrations`, `npm run validate:rls`, `npm run validate:repository-isolation`, `npm run validate:project-intelligence`, `npm run typecheck`, and opt-in `npm run validate:live-supabase` passed. Live scripts are intentionally excluded from public PR CI until protected production secrets are available. | Fixed and live-verified for focused M5 scope |
| M5M6-DEF-027 | S1 | Product Designer, Product QA | Auth recovery and feedback | Auth pages show success only after confirmed success, and password recovery lets users set a new password from the recovery session. | Signup/reset notices could appear after handled Supabase failures, and reset links initially fell back to the homepage until the exact update-password redirect was allow-listed. | Auth hooks returned `Promise<void>` and Supabase redirect allow-list lacked `/auth?mode=update-password`. | Auth actions now return typed success/failure results, AuthPage shows success notices only on `ok`, recovery links target `/auth?mode=update-password`, recovery sessions can call `updateUser({ password })`, immediate update feedback is shown, and Supabase auth redirects include the exact update URL. | Local auth tests, `validate:auth-redirects`, typecheck, lint, full tests, build, PR #7/#8 CI, and merged main CI passed. Live disposable recovery landed on update form, returned `PUT /auth/v1/user` 200, and sign-in with rotated password passed. | Fixed and live-verified for disposable recovery |
| M5M6-DEF-028 | S1 | Principal Engineer, Product QA | CI production smoke | Pull-request CI must not fail against old production when testing unmerged branch changes, while `main` still requires live production smoke. | PR #4 production smoke tested `https://azure-quest-pwa.vercel.app`, so branch fixes could not affect the target until after merge/deploy. | Production smoke used the repository production URL variable on pull requests and did not distinguish deployed-production verification from branch validation. | `scripts/run-production-smoke.mjs` now reports `PRODUCTION_SMOKE_STATUS=SKIPPED_PULL_REQUEST` on pull requests and keeps `main` release-blocking when `PRODUCTION_BASE_URL` is missing or smoke fails. | `npm run validate:production-smoke-gate` passed locally. | Fixed in repo; PR/main CI pending |
| M5M6-DEF-029 | S2 | Security/Data/Governance QA | Project Intelligence ownership | Project Intelligence analyses must reference an imported project owned by the same user at the database layer. | `project_intelligence_analyses.user_id` was RLS-scoped, but `imported_project_id` referenced only `imported_projects(id)`. | The schema lacked a composite ownership invariant between analysis rows and imported project rows. | Added migration `0026_project_intelligence_owner_fk.sql` with unique `(id, user_id)` on imported projects and a composite FK from analyses to imported projects. | Migration validation, production apply, remote history verification, and live User A/User B Project Intelligence isolation passed on 2026-08-03. | Fixed, applied live, and focused two-user verified |
| M5M6-DEF-030 | S2 | Security/Data/Governance QA | Local learner persistence | Same-browser account switching must not expose one authenticated learner's local attempts, flags, repository imports, Project Intelligence drafts, or interview/assessment recovery data to another authenticated learner. | Zustand/localForage wrote learner data to shared `praxisgrid:*` keys, so two signed-in users on the same browser could hydrate merged local state before cloud/RLS boundaries applied. | App store hydration ran before AuthProvider could provide a Supabase user ID, and all local persistence actions wrote global localForage keys. | Added owner-scoped localForage keys for authenticated users, reran hydration when the auth user changes, blocked signed-in fallback to anonymous legacy data, kept Azure Quest legacy migration anonymous-only, scoped local reset to the active owner partition, added unit coverage, and wired `validate:local-user-isolation` into CI. | `npm run validate:local-user-isolation`, `npm test -- src/store/useAppStore.test.ts`, and `npm run typecheck` passed locally. Full PR CI and live signed-in account-switching browser verification pending. | Fixed in repo; live browser verification pending |
| M5M6-DEF-020 | S2 | Security/Data/Governance QA | Content-quality reports | Question/content reports require authenticated users under the mandatory-auth product model. | RLS allowed anonymous report inserts through `(auth.uid() is null or user_id = auth.uid())`. | The earlier logged-out demo-report model was not updated after mandatory auth. | Added migration `0021_auth_required_content_quality_reports.sql` to replace the insert policy with authenticated-only `auth.uid() is not null and user_id = auth.uid()`. Updated the content-quality validator to require the hardening migration. | Live User A/User B content-quality report isolation and Support Admin report read checks passed on 2026-08-03. Temporary reports were explicitly deleted after verification. | Fixed and live-verified for focused M5 scope |
| M5M6-DEF-021 | S1 | Product QA | Personal/demo project removal | New users see no projects connected; production Career Lab does not import fixture project stories; synthetic fixtures live only in isolated test paths. | Career Lab preselected fixture projects and rendered fixture project cards before repository import. | The earlier Career Lab demo mapper remained active after the private Project Intelligence direction changed. | Removed production `projectStories` usage from Career Lab, defaulted selected projects to empty, made imported repositories the only mapper evidence source, added a no-projects-connected empty state, created isolated `Northstar Inventory API` test fixture, and added `validate:project-fixtures` to block production fixture imports and prohibited owner-project identifiers. | `npm run validate:project-fixtures` and `npm run typecheck` passed locally. Full retest pending. | Fixed in repo; browser verification pending |

## Retest Protocol

Each defect must be retested with the narrowest meaningful check and then with the full required validation suite before final sign-off.
