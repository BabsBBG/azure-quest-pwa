# M5/M6 Defect Ledger

Date opened: 2026-07-26

Status: OPEN

No S1 or S2 defect may remain before M5 production hardening or Phase 6 completion sign-off.

| ID | Severity | Reporter | Route or subsystem | Expected behaviour | Actual behaviour | Root cause | Fix | Retest result | Final status |
|---|---|---|---|---|---|---|---|---|---|
| M5M6-DEF-001 | S1 | Principal Engineer, Product QA | `/admin` | Admin routes require authenticated role checks before protected data/actions render. | `/admin` was publicly routable and presented protected admin language/actions without runtime auth/role gating. | Admin Review Studio was a static scaffold mounted directly in `App.tsx`. | Added `ProtectedAdminRoute`, denied anonymous/users before Admin renders, read server-backed `user_roles`, and added static route validation. | Focused auth tests, route check, lint, and build passed locally. Live role-browser verification pending. | Fixed in repo; live verification pending |
| M5M6-DEF-002 | S1 | Principal Engineer | Source-grounded content | Trusted certification practice is served from live approved source-grounded records. | Approved records are local/static fixtures and the 600-question seed bank remains the practical learner bank. | M5 source pipeline is implemented as local scaffolding and migrations, not production ingestion/review/publish runtime. | Pending. | Pending. | Open |
| M5M6-DEF-003 | S1 | Security/Data/Governance QA | Supabase/RLS | Migrations are applied and RLS boundaries are live-tested with MAIN_ADMIN, CONTENT_REVIEWER, SUPPORT_ADMIN, USER_A, and USER_B. | SQL exists, but live Supabase migration/RLS verification is externally blocked. | Supabase CLI/project access and configured test users are not available in this baseline. | Pending. | Pending. | Open |
| M5M6-DEF-004 | S1 | Principal Engineer, Security QA | GitHub import | Repo import limits/cache are durable and scoped to authenticated users. | API uses in-memory maps and IP-based rate buckets that reset per server instance. | Serverless endpoint had no durable store/session-aware limiter. | Replaced process-memory cache/rate buckets with authenticated Supabase JWT verification, `github_import_events` per-user daily limit tracking, `github_import_cache` repo cache storage, server-side service-role-only writes, placeholder env names, and CI validation. | Focused GitHub import controls validator, authorization validator, client tests, typecheck, and lint passed locally. Live Supabase migration/env verification pending. | Fixed in repo; live verification pending |
| M5M6-DEF-005 | S1 | Product Designer, Product QA | Secondary practice surfaces | Every assessment-like public surface shows trust copy before start and hides answers/explanations until completion. | KQL Gym and other secondary practice surfaces could show explanations immediately or lack the shared pre-start notice. | Secondary learning/practice routes were not brought under the same assessment trust contract. | Added `QuestionBankNotice` to KQL Gym, Case Files, and Scenario Player; changed KQL Gym to hide correctness/explanations until completion; removed celebratory confetti from KQL completion. | Focused KQL/notice tests passed. Full suite and browser verification pending. | Fixed in repo; browser verification pending |
| M5M6-DEF-006 | S2 | Product Designer | Mobile learner IA | Mobile learner navigation uses exactly Learn, Practise, Prove and never wraps at 320px. | Existing mobile nav remained crowded and still carried more destinations. | Old M1 navigation model remained active. | Replaced primary nav with Learn, Practise, Prove and moved Account to the header. | Focused route check, lint, build, and mobile Playwright gate passed locally. | Fixed in repo; live verification pending |
| M5M6-DEF-007 | S2 | Product Designer | Career Lab / Interview Studio | In-progress interview work is recoverable. | Active answers/timer/session state were component-local and persisted only on completion. | Career Lab lacked active session persistence and recovery flow. | Added `ActiveInterviewSession`, local/legacy storage keys, cloud sync helpers, owner-only `active_interview_sessions` RLS migration, Career Lab resume/discard UI, debounced answer/rubric/score draft persistence, timed checkpoints, completion cleanup, regression test, and CI validator. | `npm run validate:active-interview-recovery`, `npm test -- src/store/useAppStore.test.ts`, `npm run typecheck`, `npm run lint`, and `npm run build` passed locally. Browser refresh/resume and live Supabase verification pending. | Fixed in repo; browser/live verification pending |
| M5M6-DEF-008 | S2 | Product QA | Test gates | Browser, mobile, WebKit, axe, visual, and production smoke gates run in CI. | Current QA is Vitest plus static validators; no Playwright/axe/visual gate exists. | Runtime verification infrastructure had not been added. | Added Playwright configs, Chromium/WebKit/mobile/tablet/desktop projects, axe accessibility checks, visual-tagged auth layout checks, public-route routing checks, production-smoke config, and CI workflow commands. | Local slices passed: Chromium 10/10, WebKit 10/10, mobile 20/20, accessibility 36/36, visual 6/6, tablet+desktop 20/20. PR GitHub Actions run `30214329194` passed. Aggregate `npm run test:e2e` exceeded the local shell timeout, but equivalent project/tag coverage passed. | Fixed in repo; production smoke pending after deployment |
| M5M6-DEF-009 | S2 | Security/Data/Governance QA | Support role boundary | SUPPORT_ADMIN can access support queues without editorial publishing authority. | Content-quality report RLS is review/admin-oriented and does not cleanly expose a support queue boundary. | Role capabilities were not separated consistently across content-quality tables or Admin affordances. | Added `can_read_support_queue`, support-only read policies for report tables, guard triggers preventing SUPPORT_ADMIN report/content mutation, role-filtered Admin queues, support-only inspect affordance, Main Admin-only approve affordance, and `validate:support-boundary`. | `npm run validate:support-boundary` and `npm run typecheck` passed locally. Live Supabase RLS verification pending. | Fixed in repo; live verification pending |
| M5M6-DEF-010 | S2 | Security/Data/Governance QA | Privacy workflows | Users can export/delete account-owned cloud data and repository analyses safely. | Local export/reset exists, but cloud export/delete workflows and verification are missing. | M3/M4 sync tables were added without full M6 privacy operations. | Added signed-in Settings actions for cloud export and cloud learning/repository-analysis deletion, added `exportCloudData` and `deleteCloudLearningData`, added owner delete RLS policies for learning/profile tables plus existing Project Intelligence/import delete policies, and added `validate:privacy-workflows`. | `npm run validate:privacy-workflows`, `npm run typecheck`, `npm run lint`, `npm test`, and `npm run build` passed locally. | Fixed in repo; live Supabase/RLS verification pending |
| M5M6-DEF-011 | S2 | Product/UX QA | Production auth | Public production has configured Supabase auth and no logged-out demo practice. | Vercel env list reports no Supabase env vars, and the app still supported logged-out local demo practice. | Production environment and route protection were not configured for mandatory auth. | Added public `/auth` and protected learner shell so unauthenticated learner routes redirect to signup. Production Supabase env setup remains blocked. | Focused auth tests, route check, lint, and build passed locally. Production smoke and live auth pending. | Partially fixed in repo; production env blocked |
| M5M6-DEF-012 | S3 | Principal Engineer | Bundle performance | Main production bundle is reasonably split. | Vite reports a main JS chunk above 500 kB. | Routes and admin surfaces are not lazy-loaded. | Pending. | Pending. | Open |
| M5M6-DEF-013 | S1 | Product Designer | Home IA | Home presents exactly three primary outcomes: Learn, Practise, Prove. | Home led with certification path cards and path-first decisioning. | Previous path-selection dashboard remained the root protected home surface. | Rebuilt `PathHome` so the first primary choices are exactly Learn, Practise, and Prove; moved certification progress to a secondary panel; added `validate:auth-first-ia`. | `npm run typecheck` passed locally. Full retest pending. | Fixed in repo; browser verification pending |
| M5M6-DEF-014 | S1 | Product Designer, Product QA | Assessment Shell and Results | Assessments use a separate focused shell and results use a compact analytical report. | `/arena` ran inside the learner shell and rendered results inside the same dashboard-style component. | The original practice arena owned both runtime and result states inside normal learner chrome. | Added `ProtectedAssessmentRoute` so `/arena` is authenticated but outside learner layout; replaced the completed-state UI with an analytical report containing Back to Practise, JSON/CSV/print exports, retake, summary, score ring, threshold explanation, correct/total, time used, domain-performance table, recommendation, and expandable question review; added `validate:assessment-shell`. | `npm run typecheck` passed locally. Full retest pending. | Fixed in repo; browser/visual verification pending |
| M5M6-DEF-015 | S1 | Product Designer, Product QA | Account/Auth copy | Auth/account copy reflects mandatory account creation and does not promise logged-out demo practice. | Account copy said local demo practice was available without sign-in. | Earlier local-first auth copy was not updated after the mandatory-auth decision. | Replaced logged-out/local-demo language in Account with mandatory-account copy and added `validate:auth-first-ia` to prevent regression. | `npm run typecheck` passed locally. Full retest pending. | Fixed in repo; browser verification pending |
| M5M6-DEF-016 | S2 | Product Designer | Global typography | Body UI uses readable sans-serif typography with monospace reserved for code/technical metadata. | `src/styles.css` still makes the whole app monospace-first. | M1.6 visual direction used developer/security monospace globally; latest production UX brief supersedes it. | Switched root/body typography to a readable sans-serif stack, retained the existing monospace stack only through `code`, `kbd`, `samp`, `pre`, `.font-mono`, and `.aq-technical`, marked Career Lab content hashes as technical metadata, and added `validate:typography` to CI. | `npm run validate:typography`, `npm run typecheck`, `npm run lint`, `npm test`, and `npm run build` passed locally. | Fixed in repo; browser/design verification pending |
| M5M6-DEF-017 | S2 | Principal Engineer, Security QA | Project Intelligence | Imported repositories produce private durable analysis records with delete/regenerate/isolation flows. | Public repo import creates deterministic draft story data and cloud upserts, but no durable analysis job/workspace/deletion workflow exists. | M4 import scope stopped at README/language story drafting and did not implement Phase 6 Project Intelligence operations; analysis IDs were content-hash-based before cloud persistence and could collide globally if two users imported the same repo. | Added typed Project Intelligence analysis payloads with overview, evidence-backed architecture map, strengths, risks/improvements, and interview prompts; persisted analyses into owner-scoped `project_intelligence_analyses` with user-scoped analysis row IDs, RLS, and cascade delete; added owner delete policy for imported projects; added Career Lab review/regenerate/delete controls; removed named demo-project coaching references from the interview path; and added `validate:project-intelligence`. | `npm run validate:project-intelligence`, `npm run validate:project-fixtures`, `npm run typecheck`, `npm run lint`, `npm test`, and `npm run build` passed locally. Latest row-isolation hardening retest passed `npm run validate:project-intelligence` and `npm run typecheck`. | Fixed in repo; live Supabase/browser verification pending |
| M5M6-DEF-018 | S2 | Principal Engineer | CI production smoke clarity | CI clearly distinguishes skipped production smoke from passed production smoke. | Production smoke only runs on `main` with `PRODUCTION_BASE_URL`; branch CI can be green without live production smoke. | Workflow condition is correct for safety but the harness/reporting can be misread as a production-smoke pass. | Replaced the silent workflow-level condition with `scripts/run-production-smoke.mjs`: PRs without a production URL print `PRODUCTION_SMOKE_STATUS=SKIPPED_MISSING_BASE_URL`, `main` fails if the URL is absent, configured runs execute Playwright, and `playwright.production.config.ts` no longer falls back to the historical Azure Quest URL. Added `validate:production-smoke-gate` to CI. | `npm run validate:production-smoke-gate`, `npm run test:production-smoke` without a production URL, `npm run typecheck`, `npm run lint`, `npm test`, and `npm run build` passed locally. | Fixed in repo; production smoke still pending until canonical URL/env are configured |
| M5M6-DEF-019 | S2 | Security/Data/Governance QA | GitHub import quota | Public repo import daily limit is enforced atomically per authenticated user. | The endpoint counted existing import events and inserted a new event later, allowing concurrent requests to exceed the daily quota. | Quota check and event write were split across two serverless/database operations; the first RPC version accepted `p_user_id` without execute-scope hardening. | Added `claim_github_import_quota` Supabase RPC with transaction-scoped advisory lock, moved the API to claim quota through that function for cached and fresh imports, added caller mismatch protection if execute is ever broadened, revoked execute from anon/authenticated, granted execute to service_role, and updated the GitHub import validator to reject split check/record helpers and require RPC hardening. | `npm run validate:github-import-controls` and `npm run typecheck` passed locally. Live Supabase RPC verification pending. | Fixed in repo; live verification pending |
| M5M6-DEF-022 | S2 | Security/Data/Governance QA | Content-quality report audit events | Report audit events are append-only and cannot be updated or deleted by privileged users. | The original report-event policy granted `for all` to Main Admin, which made audit rows mutable. | Audit events were modelled as admin-managed data instead of append-only governance records. | Added migration `0025_rpc_and_audit_hardening.sql` to drop the `for all` policy and replace it with Main Admin insert/select-only event policies while preserving support/main read policies from the support boundary migration. Updated `validate:content-quality-reports`. | `npm run validate:content-quality-reports` and `npm run typecheck` passed locally. Live Supabase RLS verification pending. | Fixed in repo; live verification pending |
| M5M6-DEF-023 | S2 | Product/UX QA | Auth redirect continuity | Signing in from a protected deep link returns the user to the intended route after email sign-in or Google SSO. | Protected routes stored `state.from`, but AuthPage always navigated authenticated users to `/`, and Google OAuth redirected to `/account`. | Auth redirect handling did not preserve route intent through the public auth page or external OAuth redirect. | Added safe internal `from` query preservation from protected routes, AuthPage return-path sanitization, Google SSO redirect target support, `validate:auth-redirects`, and an OAuth redirect regression test. | `npm run validate:auth-redirects`, focused auth tests, and `npm run typecheck` passed locally. Full retest pending. | Fixed in repo; browser verification pending |
| M5M6-DEF-020 | S2 | Security/Data/Governance QA | Content-quality reports | Question/content reports require authenticated users under the mandatory-auth product model. | RLS allowed anonymous report inserts through `(auth.uid() is null or user_id = auth.uid())`. | The earlier logged-out demo-report model was not updated after mandatory auth. | Added migration `0021_auth_required_content_quality_reports.sql` to replace the insert policy with authenticated-only `auth.uid() is not null and user_id = auth.uid()`. Updated the content-quality validator to require the hardening migration. | `npm run validate:content-quality-reports` and `npm run typecheck` passed locally. Live Supabase RLS verification pending. | Fixed in repo; live verification pending |
| M5M6-DEF-021 | S1 | Product QA | Personal/demo project removal | New users see no projects connected; production Career Lab does not import fixture project stories; synthetic fixtures live only in isolated test paths. | Career Lab preselected fixture projects and rendered fixture project cards before repository import. | The earlier Career Lab demo mapper remained active after the private Project Intelligence direction changed. | Removed production `projectStories` usage from Career Lab, defaulted selected projects to empty, made imported repositories the only mapper evidence source, added a no-projects-connected empty state, created isolated `Northstar Inventory API` test fixture, and added `validate:project-fixtures` to block production fixture imports and prohibited owner-project identifiers. | `npm run validate:project-fixtures` and `npm run typecheck` passed locally. Full retest pending. | Fixed in repo; browser verification pending |

## Retest Protocol

Each defect must be retested with the narrowest meaningful check and then with the full required validation suite before final sign-off.
